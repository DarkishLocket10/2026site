---
/*
  Props:
    - title?: string           // section heading (defaults to "Education")
    - items: Array<{
        years:   string;       // e.g. "2020–2025"
        institution: string;   // e.g. "Queen's University"
        details: string;       // e.g. "Bachelor of Computing (Honours)…"
      }>
*/
type EducationItem = {
  years: string;
  institution: string;
  details: string;
};

const {items = [] }: { title?: string; items?: EducationItem[] } = Astro.props;
---

<section id="education" class="mx-auto w-full max-w-screen-2xl px-6 md:px-8 xl:px-12 2xl:px-16 py-6
  opacity-0 -translate-x-6
  transition-all duration-700 ease-out
  motion-reduce:opacity-100 motion-reduce:translate-y-0
  "
    data-fade>
  <h2 class="text-4xl font-semibold mb-6">Education</h2>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
    {items.map(({ years, institution, details }) => (
      <div class="relative pl-8">
        {/* angled purple bar */}
        <div
          class="absolute inset-y-0 left-0 w-4 bg-accent transform -skew-x-12"

          
        ></div>

        {/* content sits above the bar */}
        <div class="relative z-10">
          <span class="text-sm ">{years}</span>
          <h3 class="!mt-2 !text-3xl font-semibold ">
            {institution}
          </h3>
          <h4>
          {details}
          </h4>
        </div>
      </div>
    ))}
   
  <style>

h4 {
  font-size: clamp(.9rem, calc(.7rem + 1vw), 1rem);
  font-weight: 400;
  margin-top: -.25rem;
}

</style>

  </div>
</section>
